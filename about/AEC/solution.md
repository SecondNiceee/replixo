# AEC — Решение (что мы сделали)

## Идея в одном предложении

У нас есть **чистая цифровая копия** того, что играет на выходе машины (микс
голосов всех участников — «дальний конец», far-end reference). Адаптивный фильтр
оценивает, **как именно** этот сигнал просачивается в захваченный системный
звук, и **вычитает** его — оставляя только настоящий контент экрана.

```
системный звук экрана ──▶ [input 0] ╲
                                       ▶ ScreenAEC worklet ──▶ MediaStreamDest ──▶ публикуемый трек
голоса участников (reference) ─▶ [input 1] ╱
```

## Файлы

| Файл | Роль |
|------|------|
| `public/aec-worklet.js` | AudioWorklet — сам алгоритм эхоподавления (DSP). |
| `lib/screen-audio-aec.ts` | Сборка Web Audio графа, подключение input 0/1, логирование метрик. |
| `lib/audio-unlock.ts` | Общий `AudioContext` и tap на микс удалённых голосов (reference-нода). |

Всё **best-effort**: если `AudioContext`, worklet или reference недоступны —
`createScreenShareAEC` возвращает `null`, и публикуется сырой трек (без
регрессии).

## Алгоритм

**Partitioned frequency-domain NLMS** (Multi-delay block frequency-domain
adaptive filter, MDF) через overlap-save с собственным radix-2 FFT и
gradient-constraint (чтобы оценка была истинной линейной свёрткой).

- `BLOCK = 128` (квант рендера AudioWorklet), `N = 256` (overlap-save).
- `TAIL_MS = 250` — длина хвоста фильтра. Покрывает реальную задержку
  вывод+loopback от ~20 мс (проводные) до ~200 мс (Bluetooth). Слишком короткий
  хвост — самая частая причина, почему AEC «ничего не делает».

### Ключевое отличие от спикерфонного AEC

- **Адаптируемся НЕПРЕРЫВНО**, пока играет reference. Никакой заморозки на
  double-talk.
- Контент экрана статистически **не коррелирует** с голосами. Поэтому NLMS
  естественно сходится только на коррелированной части (эхо), а контент
  воспринимает как градиентный шум с нулевым средним, который усредняет leakage.
- На выходе — **только линейный остаток** (без спектрального гейтинга), поэтому
  контент проходит нетронутым: нет «музыкального шума» и «качания».
- Маленький шаг `MU = 0.2` + утечка `LEAK = 0.99995` → чисто, медленно, стабильно
  и с отслеживанием дрейфа громкости/тактовой частоты ОС.

## Что чинили в этой итерации (борьба с расхождением фильтра)

**Симптом в логах:** ERLE стартует хорошо (+20 дБ), потом падает в минус
(−13 дБ) и застревает. Отрицательный ERLE = фильтр расходится и **сам добавляет
энергию** (то самое эхо, которое слышно).

Два исправления в `public/aec-worklet.js`:

### 1. Более частый gradient-constraint

Раньше constraint применялся к **одной партиции за блок** (round-robin). При
~86 партициях полный проход занимал ~250 мс — за это время в непроконстрейненных
партициях копится **aliasing циклической свёртки**, и фильтр расходится.

Теперь: `CONSTRAIN_PER_BLOCK = ceil(PARTITIONS / 16)` — несколько партиций за
блок, полный проход за ~16 блоков (~45 мс). Aliasing не успевает накопиться.

### 2. Детектор расхождения (divergence guard)

Линейный AEC **никогда не должен делать сигнал громче**. Сглаженно (EMA)
сравниваем мощность входа и выхода:

- `DIV_EMA = 0.9` — сглаживание детектора;
- `DIV_RATIO = 1.06` — если выход громче входа больше чем на 6% → расхождение;
- `DEADAPT = 0.5` — при расхождении веса умножаются на 0.5 за блок.

Что происходит при срабатывании:
1. **Bypass** — публикуется сырой захваченный звук (никакой регрессии, хуже
   сырого уже не будет).
2. **Быстрое «сдувание» весов** к нулю → NLMS переконвергируется с чистого
   состояния, а не залипает в отрицательном ERLE.

## Итерация 2 — нелинейный подавитель остаточного эха (RES)

**Симптом в логах:** ERLE стабильно положительный (+6…+18 дБ), расхождения нет —
линейный фильтр здоров. Но пользователь **всё равно слышит себя**.

**Причина:** линейного подавления в браузере реально хватает лишь на ~10–20 дБ.
Этого **недостаточно**: собственный голос, задержанный на ~200 мс и приглушённый
всего на 15 дБ, ухо отлично слышит как эхо. Особенно ярко в сценарии «делюсь
окном без медиа» — в loopback попадают **только голоса участников**, то есть
чистое эхо, которое линейный фильтр давит лишь частично.

**Решение (как в WebRTC):** после линейного AEC добавлен нелинейный
пост-фильтр — **residual echo suppressor**. Это Wiener-усиление по каждому
частотному бину, управляемое оценкой эха `|Y[k]|²` из линейной стадии:

$$G[k] = \frac{S[k]}{S[k] + \beta \cdot |Y[k]|^2}$$

где `S[k]` — мощность выхода после линейной стадии, `β = RES_OVERSUB`.

Ключевое свойство — **самогейтинг по контенту**:
- бин с настоящим контентом экрана → большой `S[k]` → `G[k] ≈ 1` → контент
  проходит **нетронутым** (нет «музыкального шума» и «качания»);
- эхо-доминантный бин (голоса без контента) → малый `S[k]`, большой `|Y[k]|²`
  → `G[k]` падает к полу → остаток эха **вырезается**.

Параметры в `public/aec-worklet.js`:

- `RES_ENABLED = true` — включён по умолчанию;
- `RES_OVERSUB = 1.6` — коэффициент пере-вычитания оценки эха;
- `RES_FLOOR = 0.05` — минимальное усиление (~ −26 дБ), никогда не глушим в ноль;
- `RES_ATTACK = 0.3` / `RES_RELEASE = 0.85` — сглаживание усиления: быстрая
  атака (давим эхо сразу), медленный релиз (без прорывов эха).

Важно: усиление применяется к **копии** спектра (`gr/gi`), а градиент NLMS
использует **несглаженный** линейный остаток `er/ei` — иначе фильтр сошёлся бы
неверно. `ERLE` в метриках теперь считается по реальному выходу после RES.

## Итерация 3 — кеш воркл��та и агрессивный RES

**Симптом:** ERLE в логах положительный (в среднем ~10–15 дБ, пики до 38 дБ),
но пользователь всё равно слышит себя. Провалы ERLE до −3.6 дБ.

**Две причины:**

1. **Кеш AudioWorklet.** `addModule("/aec-worklet.js")` кешируется браузером
   очень агрессивно. После каждого деплоя браузер мог грузить **старую**
   версию воркл��та — то есть свежие фиксы (RES, divergence-guard) вообще не
   применялись. Решено версионированием URL:
   `addModule("/aec-worklet.js?v=<version>")` в `lib/screen-audio-aec.ts`.
   **При каждом изменении воркл��та нужно поднять `AEC_WORKLET_VERSION`.**

2. **Недостаточная глубина подавления.** 10–15 дБ ERLE = подавление ~90%,
   остаток голоса с задержкой ~200 мс отлично слышен ухом. Для неслышимого эха
   нужно 30–45 дБ. Поэтому RES сделан агрессивнее:
   - `RES_OVERSUB` 1.6 → **3.0** (сильнее пере-вычитание оценки эха);
   - `RES_FLOOR` 0.05 → **0.015** (~ −36 дБ, глубокий рез на эхо-бинах);
   - `RES_ATTACK`/`RES_RELEASE` чуть быстрее реагируют.
   Контентные бины по-прежнему защищены самогейтингом, поэтому медиа-звук не
   портится.

**Важно про запуск:** для AEC не нужно ничего дополнительно —
`public/aec-worklet.js` отдаётся как статика Next.js, граф строится в браузере.
Обычного `pnpm build && pnpm start` достаточно; главное — версионирование URL,
чтобы у зрителей не завис старый воркл��т.

## Итерация 4 — робастная оценка эха (устойчивость к провалам линейного фильтра)

**Симптом:** ERLE в логах «пляшет» 3–40 дБ. В моменты провалов (3–5 дБ)
линейная оценка эха `|Y|²` схлопывается, RES перестаёт давить — и наружу
прорывается всплеск чужого/своего голоса. Это и есть слышимое эхо, несмотря на
в среднем положительный ERLE.

**Причина:** RES опирался только на **мгновенную** линейную оценку `|Y|²`.
Она хрупкая: как только линейный фильтр на миг теряет захват (дрейф тактовой
частоты, изменение задержки), `|Y|²` падает → подавление исчезает.

**Решение:** у нас **всегда** есть чистая цифровая копия дальнего конца, значит
мы **всегда знаем**, когда играют голоса участников — даже когда линейный фильтр
спотыкается. Добавлена робастная оценка эха:

1. Медленно адаптируемый по-бинный **коэффициент связи** (echo-path coupling):
   $$\text{coupling}[k] = \text{EMA}\left(\frac{|Y[k]|^2}{\text{refPower}[k]}\right)$$
   где `refPower[k] = denom[k]` — суммарная энергия reference по всему хвосту
   фильтра (доступна всегда, не зависит от здоровья линейного фильтра).
2. Робастная оценка мощности эха: `coupling[k] × refPower[k]`, взятая по
   максимуму с мгновенной `|Y|²` (чтобы не пропускать быстрые транзиенты),
   затем сглаженная (`echoPowSm`).
3. RES теперь управляется этой робастной оценкой, а не сырой `|Y|²`, поэтому
   подавление **не пропадает** во время провалов линейного ERLE.

Параметры в `public/aec-worklet.js`: `COUPLING_EMA = 0.985` (медленное обучение
связи), `ECHO_EMA = 0.6` (огибающая мощности эха), `RES_OVERSUB = 2.2`,
`RES_FLOOR = 0.012`. Самогейтинг по контенту сохранён: бины с реальным медиа
имеют большой `S[k]`, поэтому проходят нетронутыми.

Версия воркл��та поднята до `4-res-robust-coupling` в `lib/screen-audio-aec.ts`.

## Итерация 5 — диагностика задержки, широкий хвост и разрыв feedback-петли

**Важное уточнение от пользователя:** эхо слышно **даже в наушниках**. Значит
эхо **не акустическое** (динамик→микрофон), а **чисто цифровое**: на машине
демонстрирующего ОС при loopback-захвате подмешивает в поток WebRTC-воспроизведение
голосов участников. Наушники слушателя тут ни при чём — микс формируется на
выходе устройства демонстрирующего до наушников.

Сделано три вещи:

### 1. Разрыв feedback-петли захвата (детерминированно)
В `hooks/mediasoup/use-media-controls.ts` к констрейнтам добавлен
`suppressLocalAudioPlayback: true` (рядом с `restrictOwnAudio: true`). При
«демонстрации всего экрана со звуком» Chrome по умолчанию **переигрывает**
захваченный звук в динамики, ОС ловит его обратно в loopback → петля, которая
**усиливает** весь микс, включая голоса участников. Подавление локального
переигрывания рвёт эту петлю. Неизвестные браузеру констрейнты игнорируются —
регрессии нет.

### 2. Широкий хвост фильтра: `TAIL_MS` 250 → 400
Windows-аудиодвижок + Bluetooth-гарнитура вместе могут дать round-trip **больше
250 мс**, и тогда эхо оказывается **за пределами** окна фильтра — DSP физически
не может его вычесть. Хвост расширен до ~400 мс, чтобы худший случай попадал
внутрь.

### 3. Диагностика задержки (кросс-корреляция, только для логов)
Если истинная задержка эха больше хвоста — исправить это на лету нельзя, но
раньше это было **невидимо**. Теперь воркл��т оценивает объёмную задержку эха
кросс-корреляцией огибающих ����ощности primary↔reference и пишет в лог:

```
[v0] AEC ERLE <дБ> | tail 400ms (N part) | adapting=<bool> | echo delay ~<мс>ms (corr <0..1>) [⚠ OUT OF RANGE]
```

- `echo delay` — оценка реальной задержки эха.
- `corr` — уверенность оценки (0..1); значимо при `corr > 0.3`.
- `⚠ OUT OF RANGE` — задержка близка/превышает хвост → эхо частично вне окна,
  надо ещё увеличить `TAIL_MS`.

Диагностика **не трогает звуковой тракт** — регрессии быть не может.

Версия воркл��та поднята до `5-delay-diag-tail400` в `lib/screen-audio-aec.ts`.

## Итерация 8 — глубокий broadband-гейт в режиме чистого эха («тихо, но отчётливо слышно себя»)

**Симптом в логах:** ERLE в среднем 20–35 дБ (пики до 57), НО:
`lin ≈ 0 дБ` (часто глубоко отрицательный, до −27 дБ), `dom 0.86…0.99`,
`echo delay ~75ms (corr 0.9+)`. Пользователь всё ещё **отчётливо слышит себя,
хоть и тихо**.

**Диагноз (два факта из логов):**

1. **Линейный фильтр мёртв.** `lin ERLE ≈ 0`, а иногда −27 дБ (расходится →
   срабатывает divergence-guard → сброс весов → снова расхождение). Причина:
   loopback-эхо проходит через **jitter buffer воспроизведения WebRTC**, который
   непрерывно ресемплирует/тайм-варпит звук. Задержка эха всё время «плывёт», и
   линейный NLMS физически не может залочиться. Всю работу тянет только
   нелинейный RES.
2. **`dom` высокий (0.86–0.99), `corr` ~0.9** — движок уверенно распознал
   **чистое эхо, защищать нечего**.

**Почему всё равно слышно:** RES — это **спектральный** подавитель. Даже при
−25…−28 дБ он сохраняет формантную структуру голоса, поэтому голос остаётся
**разборчивым** («тихо, но отчётливо»). Прежний broadband-гейт доходил лишь до
`GATE_MIN = 0.04` (−28 дБ) и только при полном доминировании — этого мало.

**Решение:** так как в этом режиме защищать нечего (подтверждённое чистое эхо),
делаем broadband-гейт значительно **глубже** и включаем его **раньше**, с крутой
квадратичной характеристикой:

- `GATE_DOM_ON` 0.6 → **0.45** — гейт включается раньше;
- `GATE_MIN` 0.04 → **0.0025** (~ −52 дБ) — при полном доминировании блок
  почти в тишину;
- характеристика гейта: `echoGate = GATE_MIN + (1−GATE_MIN)·(1−t)²` — у порога
  почти не трогает звук (пограничный `dom` безопасен), но стремительно падает к
  полу по мере роста доминирования (реальный случай в логах `dom ~0.98`).

Медиа-контент по-прежнему держит `dom` низким (не коррелирует с голосами), поэтому
гейт для него **не включается** — звук фильма/игры/музыки проходит нетронутым.
Версия воркл��та поднята до `8-deep-echo-gate` в `lib/screen-audio-aec.ts`.

> Замечание: настоящее «чистое» решение — заставить работать линейный фильтр, но
> против дрейфа jitter-буфера это отдельная большая задача (дробная задержка +
> непрерывный ресемплинг). Пока линейная стадия не ловит эхо, глубокий гейт в
> режиме чистого эха — самый надёжный способ убрать разборчивость.

## Итерация 9 — гейт по «выжившей» энергии («когда говорю, пропадает и медиа»)

**Симптом:** после итерации 8 себя больше не слышно, НО когда участник говорит,
**вместе с эхом пропадает и медиа** — например, звук из демонстрируемого ролика
на YouTube. Причём демонстрируется **другая вкладка** (не replixo).

**Диагноз:** broadband-гейт закрывался **только по `dom`**, а `dom` — это метрика
**корреляции огибающих**. Когда участник говорит, эхо его голоса подмешивается в
захватываемую вкладку и создаёт сильно коррелированный всплеск огибающей → `dom`
взлетает, **даже если под ним играет настоящее медиа**. Гейт по одному `dom` не
может отличить «чистое эхо» от «эхо поверх медиа» и глушит весь блок → звук ролика
пропадает на каждой фразе.

**Решение:** ввести надёжный признак «есть ли полезный контент» — **энергию,
пережившую спектральный RES** (`survive = postResPow / primaryPow`). Пер-бинный
Wiener-фильтр уже убирает эхо-бины, поэтому то, что осталось, — это желаемый
контент:

- `survive ≈ 0` → блок был почти чистым эхом, RES его вычистил → безопасно гейтить
  остаток почти в тишину;
- `survive` высокий → большая часть энергии — НЕ эхо (медиа) → держим гейт открытым.

`contentFrac = min(1, survive / SURVIVE_REF)` (`SURVIVE_REF = 0.04`). Глубокая
цель гейта по `dom` применяется теперь **только к эхо-части** блока:

```
echoGate = contentFrac + (1 − contentFrac) · gated
```

где `gated` — прежний крутой квадратичный таргет по `dom`. Так медиа (высокий
`survive`) удерживает гейт открытым пропорционально своей энергии — звук ролика
продолжает играть сквозь речевую фразу, а чистое эхо по-прежнему уходит в тишину.
Версия воркл��та поднята до `9-survive-aware-gate` в `lib/screen-audio-aec.ts`.

## Метрики и отладка

В `lib/screen-audio-aec.ts` стоит `AEC_DEBUG = true`. Каждые ~0.5 с в консоль
пишется:

```
[v0] AEC ERLE <дБ> | tail <мс>ms (<N> part) | adapting=<bool> | BYPASS (diverging)
```

- `ERLE` — насколько давится эхо (растёт и положительный = хорошо).
- `adapting` — играет ли сейчас reference (идёт ли адаптация).
- `BYPASS (diverging)` — сработала защита от расхождения, отдаётся сырой звук.

## Как проверять / на что смотреть

- `/aec-worklet.js` — статический файл, браузер его агрессивно кеширует. После
  изменений делай **hard-refresh (Ctrl+Shift+R)** у демонстрирующего.
- Если эхо остаётся даже после фикса — присылай логи. Смотрим на:
  - `ERLE` уходит в минус? → всё ещё расходится, крутим constraint/`MU`.
  - Bluetooth-наушники? → задержка может быть >250 мс, увеличиваем `TAIL_MS`.
  - `adapting=false` постоянно? → reference-нода не отдаёт микс голосов
    (проблема в `audio-unlock.ts`, а не в самом фильтре).
